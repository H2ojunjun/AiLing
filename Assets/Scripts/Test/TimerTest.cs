using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TimerTest : MonoBehaviour
{
    
    // Start is called before the first frame update
    void Start()
    {
        //for(int i = 0; i < 3; i++)
        //{
            //int timerer = TimerManager.Instance.AddTimer(3, 5, 1, null, (a)=> { Debug.Log("call!"+ Time.realtimeSinceStartup); }, () => { Debug.Log(Time.realtimeSinceStartup); });
        //}
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
